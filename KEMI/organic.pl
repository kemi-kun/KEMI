:- module(organic,[organic_name/2]).

organic_name(Formula, Name) :-
    fail.
	% substitutive(Formula, Name);
	% functional_class(Formula, Name);
	% conjunctive(Formula, Name);
	% additive(Formula, Name);
	% subtractive(Formula, Name);
	% multiplicative(Formula, Name);
	% fusion(Formula, Name);
	% hantzsch_widman(Formula, Name);
	% replacement(Formula, Name).
